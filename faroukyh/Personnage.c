#include "player.h"


  void initializePlayer(void)
   {

       // Charge le sprite de notre Personnage
       player.sprite = loadImage("Personnage.png"); //fonction qui permet d importer notre personnage
       //que se soit link droit,link haut,link bas ,link gauche .

      // Coordonnées de démarrage de notre Personnage
       player.x = //...;
       player.y = //...;

   }
